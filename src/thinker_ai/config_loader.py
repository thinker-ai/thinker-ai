import os

import yaml

from thinker_ai.configs.const import PROJECT_ROOT

# def load_config():
#     with open(str(PROJECT_ROOT / "config.yaml"), "r", encoding="utf-8") as file:
#         yaml_data = yaml.safe_load(file)
#         if yaml_data:
#             os.environ.update(yaml_data)
#
#
# # 加载本地 YAML 文件
# load_config()
